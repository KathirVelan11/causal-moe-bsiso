# Dataset — supervisor-provided, not tracked (large, not ours to redistribute)
Dataset/
code/data_external/
code/cache/

# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
venv/

# OS / editor
.DS_Store
Thumbs.db
.vscode/

# Raw stdout captures from background training runs — content is already
# distilled into results/*.json and PROJECT_PLAN.md's Progress Log.
code/*.log
code/*.err.log
